#pragma once

namespace SquirrelConf {
	enum ItemList {
		Test = 0, // For testing

		MAX = 1 // Set this to amount of items in this enum (Does not include MAX)
	};

	enum Type {
		Eater,
		Generator
	};
}